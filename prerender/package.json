{
  "name": "prerender",
  "version": "1.0.0",
  "main": "src/app.ts",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.7",
    "copyfiles": "^2.4.1",
    "ts-loader": "^9.2.6",
    "ts-node": "^10.4.0",
    "typescript": "4.3.5",
    "xml2js": "^0.4.23"
  },
  "dependencies": {
    "ramda": "^0.27.1",
    "ramda-extension": "^0.11.0",
    "request": "^2.88.2"
  },
  "scripts": {
    "prepare": "copyfiles -f ../src/assets/configurations/config.js dist/assets/configurations && copyfiles -f ../src/sitemap.xml dist/assets",
    "build": "tsc && yarn prepare && yarn start",
    "start": "node dist/app.js"
  }
}
