export interface IPaginationConfig {
    itemsPerPage: number;
    showBoundaryLinks: boolean;
    maxSize: number;
    firstText: string;
    previousText: string;
    nextText: string;
    lastText: string;
}
